# Tika 验证记录

记录日期：2026-09-15。合并原 P0 原型与 P3 打包报告，并记录办公文档类型扩展结果。实现与复现命令见[本地办公文档读取](document-reading.md)。

## 当前状态

| 层级           | 已记录结果                                                                         | 未完成部分                                      |
| -------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------- |
| P0 Server 原型 | macOS arm64 当前探针通过                                                           | 跨平台、慢解析取消/超时、峰值资源、外部访问诱捕 |
| P1 读取服务    | 共享契约、快照、队列、缓存、签名游标及单元测试；macOS arm64 固定运行时集成测试通过 | 更完整的内容和异常样本验收                      |
| P2 Agent 接入  | 工具注册、引用路由、权限、活动显示与相关回归测试完成                               | 目标机产品验收                                  |
| P3 打包        | 三目标架构资源装配与检查已实现；macOS arm64 未签名包及内置运行时冒烟通过           | 所有平台真实安装矩阵，另两平台运行验证          |

无认证回环端口符合当前已接受的本机信任模型，不是鉴权能力。跨平台和可靠终止等剩余门槛尚未通过，不能标记为全部交付验收完成。

## 原型验证矩阵

| 项目                      | macOS arm64                                                   | macOS x64 / Windows x64 |
| ------------------------- | ------------------------------------------------------------- | ----------------------- |
| 下载、完整性与启动        | Apache PGP 签名、固定指纹、Tika SHA-512、Temurin SHA-256 通过 | 待执行                  |
| 版本与端点                | `/version` 无凭据返回 200，符合当前模型                       | 待执行                  |
| CORS 与高风险端点         | 未授予非可信来源；每请求配置 403，Pipes 404                   | 待执行                  |
| DOC、DOCX、XLS、XLSX、PDF | 当前固定小样本通过                                            | 待执行                  |
| 取消与 fork 恢复          | 客户端取消后服务响应；被杀 fork 可替换                        | 待执行                  |
| 退出进程树回收            | 当前探针无剩余 PID                                            | 待执行                  |
| 性能与包体                | 已有首轮观测，见下表                                          | 待执行                  |

探针结果写入 Git 忽略的 `.local/tika-p0-probe.json`。样本及生成方式见 [样本说明](tika-fixtures.md)；所有样本为自生成内容，DOC 是 LibreOffice Word 97 导出的真实二进制文件。

## macOS arm64 首轮数据

测试机器为 Darwin arm64。以下是可复现实测值，不是跨机器性能承诺：

| 指标                            |                            结果 |
| ------------------------------- | ------------------------------: |
| 下载归档合计                    | 106,508,235 bytes，约 101.6 MiB |
| 解压运行时合计                  | 222,498,163 bytes，约 212.2 MiB |
| Server 冷启动至 `/version` 就绪 |                       8.2–8.8 s |
| 当前小样本文本解析              |                       31–108 ms |
| 父进程加单个 fork 的观测 RSS    |                  约 258–397 MiB |

RSS 是数次运行结束前的瞬时总和，不是峰值。小样本解析耗时不包含 Server 冷启动，也不能外推到 30 MiB 或复杂文档。首次 `/detect` 会触发 fork 懒启动，当前探针尚未单独记录这段耗时。

内容观察：当前 DOC/DOCX 样本的关键中文、英文、多语言文本、页眉页脚和表格单元格顺序通过；XLS/XLSX 的中英文单元格文本和 PDF 的文本层关键标记通过。普通 DOCX 会因禁用嵌入递归出现嵌入资源限制标记，当前正文归一化不将该标记单独视为失败。

## 2026-09-15 macOS arm64 本地结果

- 固定资源准备通过 Tika PGP/SHA-512、Temurin SHA-256、Java `-version` 和 Tika launcher 检查。
- electron-builder 成功生成未签名 DMG 与 ZIP；应用资源中仅存在 `darwin-arm64` 运行时，没有携带下载归档或其他架构。
- 打包后检查使用安装包内 Temurin、Tika 和配置启动服务，并成功提取 `simple-content.docx` 的中文标题及结束标记。
- 应用目录约 906 MiB，DMG 与 ZIP 均约 384 MiB；其中未压缩 Tika + JRE 运行时为 222,498,163 bytes。尚无不含 Tika 版本的同构基线，不能把安装包增量等同于整个运行时大小。
- 自动签名尝试因本机钥匙串存在两个同名 Apple Development 身份而失败；按仓库默认配置关闭自动签名后构建通过。Developer ID 签名、公证、DMG 安装和 Gatekeeper 行为仍未验证。

## 待完成验收

| 类别              | 必测项                                                                                                                  |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 真实运行时        | 可控慢解析的取消和超时、fork 终止与恢复、整棵进程树周期采样峰值、首次检测触发的 fork 冷启动耗时                         |
| 安全与异常内容    | 外部访问诱捕、压缩展开超限；空、损坏、加密、伪装后缀与超大文件                                                          |
| 内容完整性        | Word 批注、修订、文本框、脚注尾注、合并及嵌套表格；Excel 多工作表、公式、隐藏内容；PDF 多栏、表格、扫描件和字体编码     |
| 服务回归          | 路径穿越、项目外符号链接、读取时替换和特殊文件；Unicode 分段、旧/跨项目游标及缓存淘汰；已有读取工具路由                 |
| macOS arm64 / x64 | 从 DMG 安装；无系统 Java、Docker 且断网读取；退出/重启；空格与中文路径；端口竞争；Developer ID、公证及 Gatekeeper       |
| Windows x64       | NSIS 默认及自定义空格/中文安装路径；无系统 Java、Docker 且断网读取；退出/重启；端口竞争；Defender/SmartScreen；代码签名 |
| 隐私              | 目标机日志与诊断包不含正文、元数据、原始 Tika stderr 或本机绝对文档路径                                                 |

每个平台通过 Agent 的 `document_read` 读取 DOC、DOCX、XLS、XLSX 和 PDF 固定样本。单元测试验证契约，真实 Tika 测试验证解析，安装包测试验证独立运行，结果分别记录。

验收记录包含安装包文件名与 SHA-256、系统版本、CPU 架构、系统 Java 是否存在、网络状态、样本结果、进程回收、安装包与安装后体积。失败只保存脱敏日志和错误类别，不纳入私密文档。
